

<div class="container">
  <div class="row">
        <div class="col-md-8">
          <% @posts.each do |post|%>
          <h3><%= post.title %></h3>
            <p><%= post.text %></p>
            <% end %>

          </div>

          <div class="col-md-4">
          <%= render "new" %>
        </div>
  </div>
</div>
